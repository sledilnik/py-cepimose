[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
test = "nosetests -v"
testForSledilnik = "nosetests -v -a 'sledilnik'"
fmt = "black ."

[packages]
requests = ">=2.24.0"

[requires]
python_version = "3"

[dev-packages]
nose = "~=1.3.7"
black = {git = "git://github.com/psf/black", ref = "20.8b1"}

[pipenv]
allow_prereleases = true
